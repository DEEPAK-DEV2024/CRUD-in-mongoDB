<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-slate-800 p-10">
    <div class="text-blue-500 text-xl mb-3">
        <a href="/">Go To Add User </a>
    </div>
    <h1 class="text-white text-3xl my-5">All User</h1>

    <div class="flex flex-wrap justify-start gap-5">

        <% user.forEach((user)=>{ %>
            <div class="w-72 bg-slate-700 h-80 rounded-lg px-5 py-3">
                <div class="bg-slate-600 h-44 rounded-lg p-3 overflow-hidden">
                    <img src="<%= user.imgurl %>" alt="" class="h-full w-full object-cover object-top">
                </div>

                <div class="mt-5">
                    <h2 class="text-gray-400 text-2xl">
                        <%= user.username %>
                    </h2>
                    <h3 class="text-gray-500 text-lg">
                        <%= user.email %>
                    </h3>
                </div>

                <div class="mt-2 pl-1">
                    <a href="/edit/<%= user._id %>" class="text-blue-500 ">Edit</a>
                    <a href="/delete/<%= user._id %>" class="text-red-500 ml-5">Delete</a>
                </div>
            </div>
            <% }) %>
    </div>
</body>

</html>